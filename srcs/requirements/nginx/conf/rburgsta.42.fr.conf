server {
  listen 443 ssl;
  server_name rburgsta.42.fr;

  ssl_certificate /etc/nginx/ssl/ssl.crt;
  ssl_certificate_key /etc/nginx/ssl/ssl.key;
  ssl_protocols TLSv1.3;

  root /var/www/rburgsta.42.fr;
  index index.php;

  location / {
    try_files $uri $uri/ =404;
  }
}